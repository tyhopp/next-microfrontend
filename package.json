{
  "private": "true",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "os": [
    "darwin",
    "linux"
  ],
  "engines": {
    "node": ">=18.16.0"
  },
  "scripts": {
    "export": "npm run export --workspaces --if-present"
  }
}